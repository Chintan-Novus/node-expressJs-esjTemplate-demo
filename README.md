# ExpressJS (nodeJS) using ejs template engine DEMO
- expressJS
- nodeJS
- ejs Template engine

## Setup (Install NPM Dependencies)
Once you clone this repository, run this command on your terminal to install all needed dependencies:
```sh
$ npm install
```
```sh
$ DEBUG=myapp:* npm start
```